# OpenAI LLM layer — copy to .env and fill in.
OPENAI_API_KEY=sk-...
# Optional overrides
OPENAI_MODEL=gpt-4o-mini
PORT=3001
# Comma-separated allowed origins for CORS (defaults to the Vite dev server)
CORS_ORIGIN=http://localhost:5173
